# Personal Docs Website

This repository uses Docusaurus to render markdown notes as a website.

## Prerequisites

- Node.js 18+
- npm

## Run locally

```bash
npm install
npm start
```

The site runs on `http://localhost:3000`.

## Build for production

```bash
npm run build
npm run serve
```
